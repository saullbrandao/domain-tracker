import Loader from 'react-loader-spinner'
import 'react-loader-spinner/dist/loader/css/react-spinner-loader.css'

export const Loading = () => {
  return <Loader type="ThreeDots" color="#ffffff" height={20} width={20} />
}
